# Test file for SendToRWithComments functionality

# Simple arithmetic
x <- 1:5
mean(x)

# Data inspection
head(mtcars)

# Function definition
my_func <- function(n) {
    result <- n * 2
    return(result)
}

# Function call
my_func(10)

# Multiple operations
y <- c(1, 4, 9, 16, 25)
sqrt(y)
summary(y)