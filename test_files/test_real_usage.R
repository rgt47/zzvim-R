# Test file for clean code execution
# Press <CR> on these lines to test the new clean approach

# Single line test
x <- 5

# Small function test  
my_func <- function(a) {
    result <- a * 2
    return(result)
}

# Multi-line assignment
data <- data.frame(
    id = 1:5,
    value = rnorm(5)
)