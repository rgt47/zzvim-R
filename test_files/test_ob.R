library(dplyr)
aa = head(iris)
bb = head(mtcars)
cc = 1:10


check the documentation thoroughly for accuracy and clarity. Review all the code and make sure all code and comment lines are wrapped at 76 characters to facilitate printing. 

